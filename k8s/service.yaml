apiVersion: v1
kind: Service
metadata:
  name: sre-case-app
spec:
  # NodePort: cluster icinden sre-case-app:8080, disaridan localhost:30080
  type: NodePort
  selector:
    app: sre-case-app
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
